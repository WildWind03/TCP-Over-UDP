package com.chirikhin.net;

import java.util.logging.Logger;

public abstract class AbstractMessageReceiver implements Runnable {
    private static final Logger logger = Logger.getLogger(AbstractMessageReceiver.class.getName());
}
