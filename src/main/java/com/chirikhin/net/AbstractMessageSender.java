package com.chirikhin.net;

import java.util.logging.Logger;

public abstract class AbstractMessageSender implements Runnable{
    private static final Logger logger = Logger.getLogger(AbstractMessageSender.class.getName());
}
